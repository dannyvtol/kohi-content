name: Update content in kohi-quartz
on: workflow_dispatch
jobs:
  dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatch workflow
        uses: peter-evans/repository-dispatch@v3
        with:
          token: ${{ secrets.TOKEN }}
          repository: dannyvtol/kohi-quartz
          event-type: update-submodules
